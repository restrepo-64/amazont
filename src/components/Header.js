import React from 'react'

function Header() {
  return (
    <div>Header

        {/* mapping: 
        logo on the left(img), 
        search bar, 
        sign in button/orders/account, 
        cart with a number   */}
    </div>
  )
}

export default Header